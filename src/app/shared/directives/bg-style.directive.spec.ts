import { BgStyleDirective } from './bg-style.directive';

describe('BgStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BgStyleDirective();
    expect(directive).toBeTruthy();
  });
});
